datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}


model Gyh_user {
    id              Int         @id @default(autoincrement())
    name            String
    email           String
    password        String
}

// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model languages {
  id     Int      @id @default(autoincrement())
  name   String
  sheets sheets[] @relation(name: "languagesTosheets")
}

model sheets {
  id          Int       @id @default(autoincrement())
  title       String
  code        String    @db.Text
  keywords    String    @db.Text
  language    languages @relation(name: "languagesTosheets", fields: [language_id], references: [id])
  language_id Int
  user        users     @relation(name: "sheetsTousers", fields: [user_id], references: [id])
  user_id     Int       @default(1)
}

model users {
  id       Int      @id @default(autoincrement())
  username String   @unique
  password String
  name     String?
  email    String?  @unique
  sheets   sheets[] @relation(name: "sheetsTousers")
}

model Gyh_user {
  id       Int    @id @default(autoincrement())
  name     String
  email    String
  password String
}
